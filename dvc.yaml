stages:
  train:
    cmd: python pipeline/train.py
    deps:
      - pipeline/train.py
      - data/churn.csv
    outs:
      - models/model.joblib
      - models/features.json
    metrics:
      - metrics/scores.json:
          cache: false
